import sys, os

def menu():
    divider_msg = "==========" # Set the divider message
    print(divider_msg + "\nSorry, this page is not ready yet, check back in a later update!") # Print the apology message
    sys.exit(0) # rip